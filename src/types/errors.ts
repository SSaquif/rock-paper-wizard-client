// @todo: Maybe I dont need this
// Using from API instead

// Used in the client to display error messages
// When the user enters invalid data
export type EntryError = {
  isError: boolean;
  message: string | null;
};
